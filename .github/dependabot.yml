version: 2

updates:
  - package-ecosystem: pip
    directory: /
    schedule:
      interval: weekly
    groups:
      all-deps:
        patterns:
          - "*"
    # No ignores — LiteLLM upgraded past the compromised 1.82.7/1.82.8 range.
    # Future updates are welcome. See SECURITY.md for incident history.

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
