# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/

# uv — lock file IS committed (reproducible builds on Jetson + CI)
# uv.lock

# Secrets (NEVER commit plaintext secrets)
.env
secrets/env.yaml
secrets/*.key
secrets/.env.decrypted
*.pem

# Private identity document
SOUL.md

# Data (too large for git — never commit model weights or training data)
data/stepbible/
data/tsk/
data/models/
data/*.jsonl
data/*.parquet
data/*.arrow
# Model weight formats
*.safetensors
*.gguf
*.onnx
*.onnx.data
*.pt
*.pth
*.bin
# Training checkpoints
checkpoint-*/
optimizer.pt
scheduler.pt
rng_state.pth
trainer_state.json
training_args.bin

# Logs
logs/
*.log

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
.claude/

# Tooling caches
.ruff_cache/
.mypy_cache/
.pytest_cache/
.ty_cache/
